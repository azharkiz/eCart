/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import ECart from './src/Modules/List';

AppRegistry.registerComponent(appName, () => ECart);
